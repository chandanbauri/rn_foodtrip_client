1) every list here will be a flat list with pagination

generate Production key
 1)

 sudo keytool -genkey -v -keystore main-foodadda.keystore -alias foodadda-key-alias -keyalg RSA -keysize 2048 -validity 10000